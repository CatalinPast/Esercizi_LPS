#include <stdio.h>
int main ( void )
int x , y ;
// se vuoi rifare do while con while copia corpo del do while e rifai
// identico con while e incolla anhce corpo sopra al while EQUIVALENTE
{
    // con do while quello di prima effetto collaterale che dato che fa prima volta sicuro
    // stampa prima cifra anche se numero negativo pero messo x > 0 sostituito a x != 0
    printf ( "Inserisci un numero intero: " ) ;
    scanf ( "%d" , & x ) ;
    return 0 ;
}
